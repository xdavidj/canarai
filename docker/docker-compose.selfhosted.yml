version: "3.8"

services:
  canarai-api:
    build:
      context: ../packages/canarai-api
      dockerfile: Dockerfile
    ports:
      - "8787:8787"
    environment:
      DATABASE_URL: sqlite+aiosqlite:///data/canarai.db
      API_SECRET_KEY: ${API_SECRET_KEY:-change-me-in-production}
      CORS_ORIGINS: "*"
      ENVIRONMENT: production
      SCRIPT_BASE_URL: http://localhost:8787
    volumes:
      - ./data:/app/data
      - ./canarai.toml:/app/canarai.toml:ro
    restart: unless-stopped
